# Dev configuaration
#
image:
  tag: 0.9.7
  
replicaCount: 1

service:
  port: 8080


env:
  - name: SECRET_KEY
    value: DevHelmChartSecret

ingress:
  enabled: true
  hosts:
    - host: testserver-local
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: testserver
              port:
                number: 8080
